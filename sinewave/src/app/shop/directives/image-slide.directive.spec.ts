import { ImageSlideDirective } from './image-slide.directive';

describe('ImageSlideDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageSlideDirective();
    expect(directive).toBeTruthy();
  });
});
